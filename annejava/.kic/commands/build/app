#!/bin/bash

cd "/workspaces/pib-dev/annejava" || exit

# build and push the local image
DOCKER_BUILDKIT=1 docker build . -t k3d-registry.localhost:5500/annejava:local
docker push k3d-registry.localhost:5500/annejava:local

if [ "$1" == "--deploy" ]
then
    kic deploy annejava
fi
